Resources:
  SQSReturnQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "SQSRETURNQUEUE01"

  SQSPEQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "SQSPEQUEUE01"

  SQSCLQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub "SQSCLQUEUE01"